// =====================
// GALAXY RAIDERS - runtime.js
// =====================

'use strict';

if (typeof GameLoop !== 'undefined' && GameLoop && typeof GameLoop.start === 'function') {
  GameLoop.start();
}
