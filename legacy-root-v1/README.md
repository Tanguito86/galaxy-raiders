# Legacy root version

Esta carpeta conserva la version monolitica anterior que estaba en la raiz del proyecto.

No usar estos archivos para evolucionar el juego ni para publicar en Google Play.
La fuente unica actual es `../www/`, porque `capacitor.config.json` tiene `webDir` apuntando a `www`.
